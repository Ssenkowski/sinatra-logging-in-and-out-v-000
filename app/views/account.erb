<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <%if User.is_logged_in%>
      <%= "#{@user.username}"%>
      <%= "#{@user.password}"%>
      <%= "#{@user.balance}"%>
    <%else
    <a href="/"
  </body>
</html>
